<div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
    <div class="px-4 py-6 sm:px-0">
      <h2 class="text-2xl font-bold text-gray-900 mb-6">Dashboard - <%= current_organization.name %></h2>

      <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
        <div class="bg-white overflow-hidden shadow-beuni rounded-beuni border border-gray-100 hover:transform hover:-translate-y-1 transition-all duration-200">
          <div class="p-6">
            <div class="flex items-center">
              <div class="flex-shrink-0">
                <div class="w-12 h-12 rounded-full flex items-center justify-center" style="background-color: #6B46C1;">
                  <span class="text-white text-lg">👥</span>
                </div>
              </div>
              <div class="ml-5 w-0 flex-1">
                <dl>
                  <dt class="text-sm font-medium text-gray-600 truncate">
                    Total de Funcionários
                  </dt>
                  <dd class="text-2xl font-bold" style="color: #6B46C1;">
                    <%= @employees_count %>
                  </dd>
                </dl>
              </div>
            </div>
          </div>
        </div>

        <div class="bg-white overflow-hidden shadow-beuni rounded-beuni border border-gray-100 hover:transform hover:-translate-y-1 transition-all duration-200">
          <div class="p-6">
            <div class="flex items-center">
              <div class="flex-shrink-0">
                <div class="w-12 h-12 rounded-full flex items-center justify-center" style="background-color: #10B981;">
                  <span class="text-white text-lg">🎂</span>
                </div>
              </div>
              <div class="ml-5 w-0 flex-1">
                <dl>
                  <dt class="text-sm font-medium text-gray-600 truncate">
                    Aniversários Este Mês
                  </dt>
                  <dd class="text-2xl font-bold" style="color: #10B981;">
                    <%= @birthday_this_month %>
                  </dd>
                </dl>
              </div>
            </div>
          </div>
        </div>

        <div class="bg-white overflow-hidden shadow-beuni rounded-beuni border border-gray-100 hover:transform hover:-translate-y-1 transition-all duration-200">
          <div class="p-6">
            <div class="flex items-center">
              <div class="flex-shrink-0">
                <div class="w-12 h-12 rounded-full flex items-center justify-center" style="background-color: #FB923C;">
                  <span class="text-white text-lg">📦</span>
                </div>
              </div>
              <div class="ml-5 w-0 flex-1">
                <dl>
                  <dt class="text-sm font-medium text-gray-600 truncate">
                    Envios Pendentes
                  </dt>
                  <dd class="text-2xl font-bold" style="color: #FB923C;">
                    <%= @pending_shipments %>
                  </dd>
                </dl>
              </div>
            </div>
          </div>
        </div>

        <div class="bg-white overflow-hidden shadow-beuni rounded-beuni border border-gray-100 hover:transform hover:-translate-y-1 transition-all duration-200">
          <div class="p-6">
            <div class="flex items-center">
              <div class="flex-shrink-0">
                <div class="w-12 h-12 rounded-full flex items-center justify-center" style="background-color: #EC4899;">
                  <span class="text-white text-lg">🎁</span>
                </div>
              </div>
              <div class="ml-5 w-0 flex-1">
                <dl>
                  <dt class="text-sm font-medium text-gray-600 truncate">
                    Campanhas Ativas
                  </dt>
                  <dd class="text-2xl font-bold" style="color: #EC4899;">
                    <%= current_organization.gift_campaigns.active.count %>
                  </dd>
                </dl>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <div class="bg-white shadow rounded-lg">
          <div class="px-4 py-5 sm:p-6">
            <h3 class="text-lg leading-6 font-medium text-gray-900 mb-4">
              Próximos Aniversários
            </h3>
            <% if @upcoming_birthdays.any? %>
              <div class="space-y-3">
                <% @upcoming_birthdays.each do |employee| %>
                  <div class="flex items-center justify-between">
                    <div>
                      <p class="text-sm font-medium text-gray-900"><%= employee.name %></p>
                      <p class="text-sm text-gray-500"><%= employee.department %></p>
                    </div>
                    <div class="text-right">
                      <p class="text-sm text-gray-900"><%= employee.birth_date.strftime("%d/%m") %></p>
                      <p class="text-xs text-gray-500"><%= employee.age %> anos</p>
                    </div>
                  </div>
                <% end %>
              </div>
            <% else %>
              <p class="text-sm text-gray-500">Nenhum aniversário este mês.</p>
            <% end %>
          </div>
        </div>

        <div class="bg-white shadow rounded-lg">
          <div class="px-4 py-5 sm:p-6">
            <h3 class="text-lg leading-6 font-medium text-gray-900 mb-4">
              Envios Recentes
            </h3>
            <% if @recent_shipments.any? %>
              <div class="space-y-3">
                <% @recent_shipments.each do |shipment| %>
                  <div class="flex items-center justify-between">
                    <div>
                      <p class="text-sm font-medium text-gray-900"><%= shipment.employee.name %></p>
                      <p class="text-sm text-gray-500"><%= shipment.gift_campaign.name %></p>
                    </div>
                    <div class="text-right">
                      <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium
                        <%= case shipment.status
                            when 'scheduled' then 'bg-yellow-100 text-yellow-800'
                            when 'processing' then 'bg-blue-100 text-blue-800'
                            when 'sent' then 'bg-green-100 text-green-800'
                            when 'delivered' then 'bg-green-100 text-green-800'
                            when 'failed' then 'bg-red-100 text-red-800'
                            end %>">
                        <%= shipment.status.humanize %>
                      </span>
                    </div>
                  </div>
                <% end %>
              </div>
            <% else %>
              <p class="text-sm text-gray-500">Nenhum envio registrado.</p>
            <% end %>
          </div>
        </div>
      </div>
    </div>
</div>