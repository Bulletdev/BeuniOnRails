<!DOCTYPE html>
<html>
  <head>
    <title>Beuni Gifts</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50" style="background-color: #F9FAFB;">
    <% if user_signed_in? && !devise_controller? %>
      <nav class="bg-white shadow-beuni border-b border-gray-100">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex">
              <div class="flex-shrink-0 flex items-center">
                <%= link_to root_path, class: "text-xl font-bold" do %>
                  <span style="color: #6B46C1;">Beuni</span> <span style="color: #EC4899;">Gifts</span>
                <% end %>
              </div>
              <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                <%= link_to "Dashboard", root_path,
                    class: "border-indigo-500 text-gray-900 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
                <%= link_to "Funcionários", "#",
                    class: "border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
                <%= link_to "Campanhas", "#",
                    class: "border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
                <%= link_to "Envios", "#",
                    class: "border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
              </div>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:items-center">
              <div class="ml-3 relative">
                <div class="flex items-center space-x-4">
                  <span class="text-sm text-gray-700">Olá, <%= current_user.name %></span>
                  <span class="text-xs text-gray-500">(<%= current_organization.name %>)</span>
                  <%= link_to "Sair", destroy_user_session_path, method: :delete,
                              class: "text-sm px-4 py-2 rounded-full transition-all duration-200 hover:transform hover:-translate-y-0.5",
                              style: "background-color: #6B46C1; color: white;" %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <main>
      <% if notice %>
        <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded relative mb-4 mx-4 mt-4">
          <%= notice %>
        </div>
      <% end %>

      <% if alert %>
        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded relative mb-4 mx-4 mt-4">
          <%= alert %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>